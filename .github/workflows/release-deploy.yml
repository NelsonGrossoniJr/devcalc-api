name: Release Deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy realizado com sucesso
        run: echo "Deploy realizado com sucesso"

